# Changelog

所有重要变更记录于此。开发期间采用 `Unreleased` 区域，按 Added / Changed / Fixed / Security 分类。

## Unreleased

### Added

- 初始产品、架构、契约和测试规格包。
- FastAPI 后端脚手架、健康检查、统一错误响应和结构化请求日志。
- 路径白名单、符号链接越界防护、JSON Schema 校验及带 revision 的原子文件写入。
- 工作区文件仓储、可重复运行的示例工作区生成脚本和 12 个后端测试。
- Vue 3 研究工作台、健康状态、设置与诊断页面，以及桌面和移动端导航。
- `Paper` Conda 环境、前后端依赖锁文件和根级 `make verify` 验证命令。
- 工作区路径检查、首次初始化、配置更新和重启后的本地工作区恢复。
- 项目列表、新建、编辑、归档筛选、项目统计、最近访问和空项目安全删除。
- 工作区与项目 API、领域模型、文件仓储及阶段 1 的端到端浏览器流程。
- 论文目录扫描、批量登记、单文件路径登记、瞬时 PDF 上传、无 PDF 手工记录、来源状态检测与重新关联。
- 项目论文矩阵页面，包含搜索、来源筛选、排序、列显隐和仅删除 PaperMatrix 记录。
- 论文发表载体、署名单位、发表/阅读时间、被引次数、语言、关键词、摘要与项目内分组编辑。
- 单篇论文详情页，以及使用 revision、文件锁和原子替换的 Markdown 结构化笔记自动保存。
- 每篇论文独立的问题清单，支持待回答、已回答、暂缓、标签，以及页码、章节、图表和定位说明等证据引用。
- 论文 schema v4 的稳定分析条目，覆盖研究问题、方法、挑战、实验、发现、局限和成立条件，并支持结构化属性、写作用途与统一证据引用。
- 分析条目增加向下兼容的研究背景与经典文献类型，避免把背景误标为研究问题、把相关工作误标为本文方法。
- 论文详情的分析条目工作区，可新增、编辑和删除条目，并明确区分已有证据与待补证据。
- 结构化 Markdown 笔记候选解析与审阅导入：保留来源区段和行号，关联证据表，支持逐条选择、重复提示及笔记/论文双 revision 冲突保护。
- 完整 Markdown 条目化：论文 schema v5 为确认条目保存固定章节、章节内顺序、稳定来源锚点和来源笔记 revision，并兼容迁移 v1–v4 记录。
- 候选确认会在三级或四级语义标题前写入不影响渲染的 `papermatrix:item` 标记；重新解析从标记恢复 item ID，重复执行不会重复插入。
- 完整文档/条目双模式编辑同一份结构化 Markdown；条目保存只替换稳定锚点对应片段，并同步论文 YAML 投影。
- 条目来源 SHA-256 指纹和同步状态：外部修改进入候选差异审阅，明确确认后才更新分析投影。
- 结构化笔记保存后自动返回解析候选和待审阅数量；每个有内容的三级/四级语义标题块形成一个条目，表格整体保留在所属标题块中。
- 面向用户和维护者的结构化笔记条目模式说明，覆盖候选提取、稳定锚点、局部写回、版本冲突与外部修改审阅。
- 个人补充笔记保存为独立 Markdown 文件，具备独立 revision、自动保存、冲突提示和浏览器本地草稿恢复，不参与结构化候选解析。
- 新论文登记时立即创建默认结构化笔记，并仅在首次创建时把已有标题、作者、单位、载体、日期、引用、语言、关键词、分组和摘要写入第 0 节。
- 候选差异审阅支持确认删除正文中已移除的旧投影；条目模式支持单条选择、全选和批量删除对应 Markdown 标题块与 YAML 投影。
- 结构化笔记条目化与领域问题归纳的完整产品方案：保留当前模板的完整 Markdown，增加个人补充笔记、稳定条目锚点、条目关系、多个问题归纳板，以及每篇论文“方法/解决程度”双列贡献矩阵。
- 重点收藏视图：已确认条目可加入与完整文档、条目模式并列的收藏页，收藏状态由稳定条目投影保存并可快速定位回 Markdown 正文。

### Changed

- 将 Codex 主提示词、计划和任务集中到本地忽略的 `.codex-prompts/`，根目录保留并忽略可自动发现的 `AGENTS.md`，同时忽略 `examples/` 示例数据。
- 根据 `ui-ux-pro-max` 建立适合研究工具的设计系统，并采用本地字体和克制的学术工作台视觉。
- 将 `.codex-prompts/DESIGN.md` 纳入后续开发的必读产品与原型参考。
- 所有 Excel、笔记、汇报和综合分析模板统一保存在根目录 `templates/`；开发完成标准增加模板同步核对。
- 校准 `.codex-prompts/` 当前基线：阶段 0–2 已完成、阶段 3 任务已细化、Excel 为七表派生模板，并固化编辑窗口防误关闭规则。
- 将阶段 4 重构为 4A–4E 的证据驱动分析核心：分析集合、方法/挑战/实验比较、关系图谱、共识/冲突/研究空白和综合分析工作台。
- 增加跨论文分析正式设计，明确权威数据边界、实验可比性、主张证据、可视化与等价表格以及综合分析防覆盖规则。
- 明确 Excel 总表对应的完整矩阵必须在软件内作为项目总览主界面，XLSX 只是该视图的导出形式之一。
- Markdown 原子写入增加 revision 冲突检查，避免并发编辑静默覆盖较新的笔记。
- 将结构化 Markdown 解析拆为候选解析和人工确认两项后续任务；未经确认的候选不得写入论文 YAML，重新解析不得覆盖人工条目。
- 论文笔记模板的解析说明更新为已实现行为；Excel 字段无需变化，确认后的候选仍使用现有分析条目列。
- Excel 模板增加论文 ID、分析条目 ID、条目类型和实验证据列，使派生表格可以追溯到 schema v4 权威条目。
- 阶段 4A 调整为先实现完整 Markdown 条目化、个人补充笔记、条目关系和稳定跳转；阶段 4B 优先实现领域问题归纳，再继续方法、挑战和实验比较。
- 候选导入响应同时返回锚点写入后的笔记，前端同步正文和 revision，避免后续自动保存用旧草稿移除稳定标记。
- 论文 schema 升级至 v6，为分析条目保存来源片段指纹，并兼容迁移 v1–v5 记录。
- 论文 schema 升级至 v7，允许研究背景与经典文献条目类型，并通过纯函数兼容迁移 v1–v6 记录。
- 论文 schema 升级至 v8，为已确认分析条目保存 `is_favorite`，并通过纯函数兼容迁移 v1–v7 记录。
- 明确区分阅读问题、论文研究问题和领域问题；论文研究问题属于结构化笔记，领域问题及论文贡献使用项目级分析文件。
- 综合分析模板增加领域问题归纳区，并强制将每篇论文的方法和解决程度分列。单篇结构化笔记模板经复核保持第 0–12 节及现有内容不变；Excel 二进制模板待对应实现切片落地时由 `TPL-401` 同步。
- 本切片已复核单篇笔记与 Excel 模板；双模式未改变模板字段或导出列，无需修改模板文件。
- “代表性顶会顶刊文献（3–5篇）”按三级父标题形成一个经典文献条目，文献 A/B/C 作为条目内部结构保存，不再拆成多个候选。

### Fixed

- 修复结构化笔记漏掉研究背景、问题重要性和四级经典文献的问题；表格后说明文字不再丢失，占位数据行不再形成有效内容。
- 修复表格每行被拆成独立分析条目的问题；旧版逐行投影在用户确认整表候选后合并，并保留人工标签、写作用途和证据。
- 补齐现有方法、流程步骤、复现信息、论文优点和可靠性检查等模板区域的候选解析，避免有内容却漏提取。
- 默认笔记模板中的框架示例和填写提示不再被误识别为可导入分析候选；未保存模板会明确提示先填写并保存。
- 单个不符合当前 Paper Schema 的论文文件不再导致整个文献矩阵加载失败；无效记录会被单独列出，并可在明确确认后仅删除 PaperMatrix 元数据和笔记。
- 损坏的 `workspace.yaml` 进入可诊断错误状态且不会被初始化流程覆盖。
- 非空或含未知内容的项目目录不会被删除。
- v2 论文记录在编辑时可原地升级为 v3，不再因旧文件的 schema 版本而拒绝保存。
- 移动端宽表只在表格容器内部横向滚动，不再带动整页左右漂移。
- 论文编辑表单兼容开发期间缺少新增字段的旧详情响应，不再把可编辑记录误报为无法读取。
- 添加论文和编辑论文窗口不再因点击外部遮罩而关闭，避免误操作丢失未保存内容。
- 笔记保存期间继续输入时会保留新草稿并再次自动保存，不会把较新的输入误标为已保存。
- 修复条目局部替换在父标题包含四级子标题时提前截断的问题；删除父条目会完整处理其子标题且保留相邻条目锚点。

### Security
