# ADR-0003：导入论文只保存引用，不创建副本

- 状态：Accepted
- 日期：2026-07-26

## 背景

用户已自行管理论文文件，明确不需要导入备份。

## 决策

导入只读取 PDF 基础元数据并保存路径、大小、mtime 和指纹。应用永不复制、移动、重命名、覆盖或删除源 PDF。

## 结果

用户移动文件后可能出现缺失状态，因此提供重新关联。项目不具备独立携带全部 PDF 的能力，但符合用户文件所有权和零副本要求。
